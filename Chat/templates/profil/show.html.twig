{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <h1>Profil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdProfil</th>
                <td>{{ profil.idProfil }}</td>
            </tr>
            <tr>
                <th>Bio</th>
                <td>{{ profil.bio }}</td>
            </tr>
            <tr>
                <th>NbrPublication</th>
                <td>{{ profil.nbrPublication }}</td>
            </tr>
            <tr>
                <th>PhotoProfilPath</th>
                <td>{{ profil.photoProfilPath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('profil_index') }}">back to list</a>

    <a href="{{ path('profil_edit', {'idProfil': profil.idProfil}) }}">edit</a>

    {{ include('profil/_delete_form.html.twig') }}
{% endblock %}
